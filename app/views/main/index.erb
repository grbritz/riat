<div class="container">
	<div class="row">
		<div class="col-md-8">
			<h1>Relation Instance Annotation Tool - <span class="small-caps">riat</span></h1>
			<p>Welcome to <span class="small-caps">riat</span>. <span class="small-caps">riat</span> is a tool that uses people to annotate relational instances. Select an experiment below to participate in <span class="small-caps">riat</span>.</p>

			<ul>
				<% @datasets.each do |set| %>
				<li><%= link_to(set.name, set.name + "/index") %></li>
				<% end %>
			</ul>
		</div>
	</div>
</div>